<UserControl x:Class="RobotNet.Windows.Wpf.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="False">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="日志 Logs"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="#18212C" />
                <TextBlock Text="{Binding LogFilePath}"
                           Margin="0,8,0,0"
                           Foreground="#5B6673"
                           TextWrapping="Wrap" />
            </StackPanel>

            <Button DockPanel.Dock="Right"
                    Width="120"
                    Height="36"
                    Margin="16,6,0,0"
                    Command="{Binding RefreshCommand}"
                    Content="刷新日志" />
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,24,0,0"
                Background="White"
                BorderBrush="#DDE3EA"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <ListBox ItemsSource="{Binding Entries}"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="13" />
        </Border>
    </Grid>
</UserControl>
